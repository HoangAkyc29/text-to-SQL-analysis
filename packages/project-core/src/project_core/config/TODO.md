# project_core.config

TODO:

- `load_project_config(root)` — read `config/project.yaml`
- Pydantic models for app settings (poll interval, feature flags, paths)
- Validate paths via `project_core.paths`
