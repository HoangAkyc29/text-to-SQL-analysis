# project_core.domain

TODO: core domain modules for your multi-agent project.

Examples (pick what fits):

- `state_store.py` — SQLite / Redis project state
- `integrations/` — external APIs, databases, file stores
- `workflows/` — domain-specific helpers used by agents
- `audit.py` — structured audit log

Keep orchestration in `libs/` — only business logic here.
