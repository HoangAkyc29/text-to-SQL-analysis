# project-test

Integrated pytest suite for the multi-agent stack (project-core, agents, MCP).

```powershell
uv run pytest packages/project-test
```

## Layout

- `unit/project_core/` — domain unit tests
- `integration/` — stack tests (TODO)
- `fixtures/` — sample platform configs (TODO)
