# commons

Truly-shared primitives used by both `agent-core` and `mcp-core`:

- `commons.logging` - structured logger factory.
- `commons.settings` - `BaseAppSettings` (pydantic-settings) for env-driven config.
- `commons.types` - `Result`, `JSONValue`, common type aliases.
- `commons.errors` - root exception hierarchy.
