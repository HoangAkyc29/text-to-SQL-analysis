# docker/

- `base/` — shared base images (`abstract-base/project-base`, `mcp-base`, `agent-base`)
- `images.yaml` — manifest mapping services → tiers

See [docs/DOCKER_BUILD.md](../docs/DOCKER_BUILD.md).
