# Default skill (template)

TODO: describe what this agent does in your multi-agent project.

## Capabilities

- `template` — placeholder capability for registry tests

## When to use

Replace this file when copying `base-agent` to a domain-specific agent package.
