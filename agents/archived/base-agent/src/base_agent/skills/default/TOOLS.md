# Tools reference (template)

## MCP tools (from platform.yaml `mcp_servers`)

TODO: list tools exposed by your MCP servers, e.g. `base_ping`.

## Native tools

| Tool | Purpose |
|------|---------|
| `example_tool` | Scaffold — replace with real tool |

Format each tool: name, params, return shape, when to call.
